<h1 align = center>ใบส่งของ/ใบแจ้งหนี้</h1>
<% if notice != "" %>
<p id="notice" class="notice"><%= notice %></p>
<% end %>

<table>
  <tr>
    <th>invoice-id</th>
    <th></th>
    <th>ชื่อลูกค้า</th>
    <th></th>
    <th>Project</th>
    <th></th>
    <th></th>
  </tr>

<% @invoices.each do |i| %>
  <tr>
    <td><%= i.id%>-<%= (i.created_at.year)+543 %></td>
    <td></td>
    <td><%= i.customer.name%></td>
    <td></td>
    <td><%= i.title %></td>
    <td></td>
    <td ><%= link_to show, ([i.customer, i]) %>|
	<%= link_to edit, edit_customer_invoice_path(i.customer, i) %>|
	<%= link_to delete, [i.customer , i ], 
	      :confirm => "Are you sure?" ,
	      :method => :delete %></td>
  </tr>
<% end %>
</table>

<!--
<%= link_to 'เพิ่มใบเสนอราคา' , newreports_path %>
-->