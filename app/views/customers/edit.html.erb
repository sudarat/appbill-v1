<h1>Edit customer</h1>
<% flash .each do |key, value| %>
  <%= content_tag(:div, value, :class => "flash #{key}") %> 
<% end %>
<%= render 'form' %>


<%= link_to 'Back', customers_path,:format => "html" %>
