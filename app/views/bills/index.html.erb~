<html>
<h1 align = center>Bills</h1>

<% flash .each do |key, value| %>
  <%= content_tag(:div, value, :class => "flash #{key}") %> 
<% end %>


<table>
  <tr>
    <th width=50px>Bill-ID</th>
    <th></th>
    <th width=100px>Customer</th>
    <th></th>
    <th width=270px>Project</th>
    <th></th>
    <th></th>
  </tr>

<% @bills.each do |b| %>
  <tr>
    <td><%= b.id%>-<%= (b.created_at.year)+543 %></td>
    <td></td>
    <td><%= b.customer.name%></td>
    <td></td>
    <td width=50px><%= b.title %></td>
    <td></td>
    <td><%= link_to delete, [b.customer , b ],:format => "html",:confirm => "Are you sure?" ,:method => :delete , :class => "right"%>
    <div class = "right">|</div>
    <%= link_to edit, edit_customer_bill_path(b.customer, b),:format => "html" , :class => "right"%>
    <div class = "right">|</div>
    <%= link_to show, ([b.customer, b]),:format => "html" , :class => "right"%>
    </td>
  </tr>
<% end %>
</table>
</html>
